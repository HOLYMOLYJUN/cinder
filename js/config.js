/* =========================================================
   config.js — 상수와 설정 테이블
   숫자를 만지고 싶으면 대부분 이 파일만 열면 된다.
   ========================================================= */

const CFG = {
  TILE: 28,               // 타일 한 변(px)
  // 화면에 보이는 타일 수. 폭이 좁은 기기에서는 줄여서 타일을 키운다
  // (캔버스가 컨테이너 폭에 맞춰 늘어나므로, 타일 수가 적을수록 크게 보인다)
  VIEW_W: 28,
  VIEW_H: 18,
  VIEW_W_NARROW: 15,
  VIEW_H_NARROW: 11,
  NARROW_AT: 760,         // 이 폭 미만이면 좁은 화면으로 본다

  ENERGY_COST: 100,       // 한 번 행동하는 데 드는 에너지
  BASE_SPEED: 10,         // 기준 속도 = 10틱에 한 번 행동

  FOV_RADIUS: 7,          // 불씨가 닿는 반경
  MONSTER_SIGHT: 8,       // 몬스터가 플레이어를 알아채는 거리

  /* 사거리. 이 셋의 간격이 곧 원거리 싸움의 전부다.

     처음에는 셋 다 6이었다. 그러면 원거리 몬스터와 마주쳤을 때
     서로 사거리 안에 있으므로 맞으면서 때리는 것 말고 할 수 있는 게 없다 —
     물러설 자리도 없고 먼저 쏠 자리도 없어서, 원거리가 근접의 느린 판본이 된다.

     그래서 사람 쪽을 벌리고 몬스터 쪽을 줄였다. 이제 그 사이의 두세 칸이
     "여기서는 내가 쏠 수 있고 저쪽은 못 쏜다"는 자리가 된다.
     겨누려면 보여야 하므로 실제 상한은 불씨 반경(FOV_RADIUS)이지만,
     겨눌 것 없이 어둠에 대고 쏘는 것은 아래 값 끝까지 날아간다. */
  RANGED_RANGE: 8,        // 사람이 던지거나 불덩이를 날리는 거리
  BOW_BONUS: 1,           // 활은 한 칸 더 — 가장 멀리 보는 것이 엘프의 정체성이다
  MONSTER_RANGE: 5,       // 원거리 몬스터가 주문을 꽂는 거리

  TOP_FLOOR: 15,
  SEWER_FLOOR: 11,         // 여기서부터 배경이 하수도로 바뀐다 (두 번째 바이옴)
  PET_FLOOR: 5,            // 이 층의 보스를 넘으면 동행을 하나 고른다
  THROW_FLOOR: 3,          // 「던지던 손」을 확정으로 주는 층
  REST_FLOORS: [3, 6, 9, 12],
  BOSS_FLOORS: [5, 10],

  MOVE_ANIM: 0.10,        // 칸 사이 미끄러지는 시간(초)
  BUMP_ANIM: 0.16,        // 공격 시 튀어나갔다 오는 시간(초)
  FLASH_TIME: 0.18,       // 피격 번쩍임 지속(초)

  SAVE_KEY: 'jaetbul.save.v1',
};

/* ---------- 방(room) 서버 ----------
   친구와 같은 방에 들어가 주고받는 것. 게임 규칙과는 아무 상관이 없다.

   HOST 가 비어 있으면 채팅이 통째로 꺼진다 — 그리고 그게 기본값이다.
   index.html 을 더블클릭해서 열었을 때(file://)도, 아티팩트 한 장으로
   구웠을 때도 네트워크는 어차피 막혀 있다. 꺼진 채로 게임이 멀쩡한 것이
   정상 동작이지 예외가 아니다.

   배포한 뒤 여기에 워커 주소를 적으면 그때부터 켜진다:
     HOST: 'https://cinder-party.<계정>.workers.dev'                     */

const NET = {
  HOST:  'https://cinder-party.vlck1111.workers.dev',

  /* 확성기와 관전만 따로 끄는 스위치. 흔적(marks)은 그대로 남는다.

     둘 다 HOST 하나에 매달아 두면 앱 빌드에서 곤란해진다 — 스토어는
     사람이 쓴 글이 남의 화면에 뜨는 순간 신고·차단 기능을 요구하는데,
     흔적은 서버에 번호만 보내서 그 요구를 애초에 안 받는다.
     같이 꺼 버리면 문제 없는 쪽까지 잃는다.

     그리고 채팅이 꺼지면 별명을 넣을 자리가 사라지므로, 흔적에 붙는 이름도
     저절로 「누군가」가 된다. 앱 빌드에는 사람이 쓴 글자가 한 톨도 없다. */
  CHAT:  true,

  PARTY: 'cinder-room',      // wrangler.jsonc 의 바인딩 이름을 kebab-case 로
  PROTO: 1,                  // 프로토콜 판 번호 — 서버와 같아야 한다

  MAX_TEXT: 200,             // 한 줄 길이 (서버도 같은 값으로 자른다)
  MAX_NAME: 16,

  PING_MS:  25000,           // 이 간격으로 한 마디 보내 연결을 살려 둔다
  DEAD_MS:  60000,           // 이만큼 아무것도 안 오면 죽은 것으로 보고 다시 붙는다
  RETRY_BASE: 800,           // 재시도 간격의 시작
  RETRY_MAX: 15000,          // 그 상한

  KEY: 'jaetbul.chat.v1',    // 방 이름과 별명을 담는 칸.
                             // 기억(save)·이어하기(run)와 수명이 다르므로 따로 둔다
};

/* ---------- 타일 ---------- */

const T = { WALL: 0, FLOOR: 1, STAIRS: 2, CAMP: 3, SHOP: 4, DOOR: 5, FORGE: 6 };

/* ---------- 색 (스프라이트 붙이기 전 임시 표현) ---------- */

/* 불빛이 닿는 곳은 따뜻하게, 기억으로만 남은 곳은 차갑게 —
   색온도만으로 "지금 보이는 것"과 "기억하는 것"이 구분된다. */
const COLORS = {
  unseen:      '#070605',
  wallLit:     '#4B3B2C',
  wallTop:     'rgba(240,170,100,.16)',
  wallDim:     '#1C1F26',
  floorLit:    '#241B14',
  floorDim:    '#0D0F14',
  floorGrid:   'rgba(0,0,0,.32)',
  stairs:      '#E9954A',
  camp:        '#E86A3A',
  shop:        '#C9A227',
  door:        '#B98A3C',
  key:         '#F0C24A',
  gold:        '#D9B04E',
  potion:      '#5FA9C4',
  player:      '#F0E2CE',
  ember:       '#E9954A',
  damage:      '#E86152',
  heal:        '#8FBF7F',
  cast:        '#9B7BD4',
  // 하수도의 형광 이끼. 회복(연두)과 헷갈리면 안 되므로 훨씬 독하게 뽑았다.
  poison:      '#A8E639',
};

/* ---------- 입력 매핑 ----------
   반드시 event.code 를 쓴다. event.key 를 쓰면 한글 입력 상태에서
   Z 가 'ㅋ' 으로 들어와 조작이 통째로 먹통이 된다.                */

const KEY_DIR = {
  ArrowUp: 'up',    KeyW: 'up',
  ArrowDown: 'down',  KeyS: 'down',
  ArrowLeft: 'left',  KeyA: 'left',
  ArrowRight: 'right', KeyD: 'right',
};

// 방향키와 함께 눌러 의도를 바꾸는 키
const KEY_MOD = {
  KeyZ: 'ranged', KeyJ: 'ranged',   // 원거리 — 아직 기억나지 않음
  KeyX: 'pass',   KeyK: 'pass',     // 공격하지 않고 이동
};

const DIRS = {
  up:    { dx: 0,  dy: -1 },
  down:  { dx: 0,  dy: 1  },
  left:  { dx: -1, dy: 0  },
  right: { dx: 1,  dy: 0  },
};

/* ---------- 층 서사 문장 ---------- */

const FLOOR_LINES = {
  1:  '문이 등 뒤에서 닫힙니다.',
  2:  '어둠 속에서 무언가가 당신을 세고 있습니다.',
  3:  '공기가 무거워집니다. 숨을 쉴 때마다 재의 맛이 납니다.',
  4:  '벽에 이름들이 긁혀 있습니다. 아직 읽을 수 없습니다.',
  5:  '그것은 당신을 보고 물러서지 않았습니다.\n기다리고 있었던 것처럼.',
  6:  '불이 이미 피워져 있습니다. 누가 피운 걸까요.',
  7:  '당신의 이름을 부르는 소리.\n목소리가 당신의 것입니다.',
  8:  '벽의 이름들이 읽히기 시작합니다.',
  9:  '그중 하나는 당신의 글씨입니다.',
  10: '그것은 이름을 가지고 있었습니다.\n당신이 붙인 이름입니다.',
  // 배경이 하수도로 바뀌는 층. 눈으로 먼저 보이지만 말로도 한 번 짚어 준다.
  11: '계단이 익숙합니다.\n벽을 타고 물이 흐르는 소리가 납니다.',
  12: '여기서 몇 번이나 돌아섰습니까.',
  13: '재가 발목까지 쌓여 있습니다.',
  14: '계단이 하나뿐입니다.',
  15: '꺼진 불 앞에, 당신이 앉아 있습니다.',
};

/* ---------- 층 속성 ----------

   층마다 성격이 하나 붙는다. 진입 문장이 그것을 흘리므로 분위기 글이 곧
   경고문이 된다 — 「발소리가 여럿입니다」를 읽고 물약을 미리 마시는 사람이
   생기면 그 문장은 제 몫을 한 것이다.

   왜 늘렸는가: 처음에는 넷이었고 그중 둘이 빈 것이라 **세 번에 한 번은
   아무 성격이 없었다.** 열다섯 층 중 다섯 층이 그냥 층이면 「이번엔 뭐지」가
   안 생긴다. 빈 것을 하나로 줄이고 종류를 늘린다 — 평범한 층도 있어야
   특별한 층이 특별해지므로 아주 없애지는 않는다.

   손잡이는 셋뿐이다(몬스터 수 · 시야 · 골드). 그래서 나머지는 이미 있는
   규칙을 비트는 쪽으로 만든다 — 없던 규칙을 새로 만들면 검사할 곳이 늘고
   밸런스가 어디서 무너졌는지 알 수 없게 된다.

   숫자를 읽는 법:
     monsterMul  이 층의 몬스터 수 배율 (기본 1.0)
     fovAdd      불씨 반경에 더할 값 — 음수면 어둡다
     goldMul     바닥에 놓이는 금 무더기 배율
     eliteMul    엘리트가 붙을 확률 배율
     potionAdd   바닥에 놓이는 물약을 더 준다
   각 층에 하나만 걸리므로 여러 개가 겹쳐 터지는 일은 없다. */

/* HUD 에 남길 짧은 이름. 진입 문구(hint)는 분위기를 흘리는 글이라 길고,
   이건 층에 머무는 내내 눈 구석에 있을 것이라 짧아야 한다. 같은 것을
   두 가지 길이로 말하는 셈이다. */
const FLOOR_TAG_NAME = {
  dense: '무리', dark: '어둠', elite: '큰 것', treasure: '쇠붙이',
  hoard: '반짝임', spring: '샘', quiet: '고요', open: '트임',
};

const FLOOR_TAGS = [
  // ---- 위험한 층 ----
  { id: 'dense',    hint: '발소리가 여럿입니다.',            monsterMul: 1.6, fovAdd: 0 },
  { id: 'dark',     hint: '불씨가 자꾸 꺼지려 합니다.',      monsterMul: 1.0, fovAdd: -3 },
  /* 수는 적은데 하나하나가 세다. 「적게 만나지만 만나면 큰일」이라
     dense 와 반대 방향의 긴장이다 — 도망칠지 붙을지를 매번 묻는다. */
  { id: 'elite',    hint: '큰 것이 지나간 자국이 있습니다.', monsterMul: 0.7, fovAdd: 0, eliteMul: 3.5 },

  // ---- 값이 있는 층 ----
  { id: 'treasure', hint: '쇠붙이 냄새가 납니다.',            monsterMul: 1.0, fovAdd: 0, goldMul: 2.2 },
  /* 어두운데 금이 많다. 위험과 보상을 한 층에 묶으면 「들어갈까 지나칠까」가
     생긴다 — 손잡이 둘을 반대로 돌리는 것만으로 판단이 하나 늘어난다. */
  { id: 'hoard',    hint: '어둠 속에서 무언가 반짝입니다.',  monsterMul: 1.2, fovAdd: -2, goldMul: 3.0 },
  /* 다친 채로 올라온 사람을 위한 층. 늘 있으면 물약을 깎은 뜻이 없어지므로
     드물어야 한다 — 그래서 목록에 하나뿐이다. */
  { id: 'spring',   hint: '물 흐르는 소리가 납니다.',        monsterMul: 0.9, fovAdd: 0, potionAdd: 2 },

  // ---- 숨 돌리는 층 ----
  { id: 'quiet',    hint: '너무 조용합니다.',                monsterMul: 0.6, fovAdd: 1 },
  /* 멀리 보이니 먼저 쏠 수 있고, 먼저 피할 수도 있다. 원거리를 쥔 사람에게는
     사냥터고 근접에게는 그냥 편한 층이다 — 같은 층이 사람마다 다르게 읽힌다. */
  { id: 'open',     hint: '천장이 높습니다. 멀리까지 보입니다.', monsterMul: 1.1, fovAdd: 3 },

  /* 아무 성격도 없는 층. 하나는 남겨 둔다 — 열다섯 층이 전부 특별하면
     특별한 것이 없는 것과 같다. 평범한 층이 다음 층의 배경이 된다. */
  { id: null,       hint: '',                                monsterMul: 1.0, fovAdd: 0 },
];
